## What this changes

## Why

## Checklist

- [ ] Every new or changed check declares the change that must make it red
- [ ] `uv run pytest`, `ruff check`, `ruff format --check` and `mypy` are clean
- [ ] `CHANGELOG.md` has an entry under Unreleased
- [ ] `docs/design.md` or `docs/plan.md` updated if behavior or plan changed
