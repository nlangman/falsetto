# The router example

Four checks, one per verdict. From the repository root:

```
pytest examples/router
```

Expected verdict line:

```
falsetto: 1 proven, 1 failed, 1 false, 1 unproven
```

Each check's comment says which verdict it earns and why.
